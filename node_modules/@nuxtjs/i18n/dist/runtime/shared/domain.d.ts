import type { I18nPublicRuntimeConfig, LocaleObject } from '#internal-i18n-types';
/**
 * Whether the locale is served on the given host
 */
export declare function isLocaleOnHost(locale: LocaleObject | undefined, host: string): boolean;
export declare function matchDomainLocale(locales: LocaleObject[], host: string, pathLocale: string): string | undefined;
export declare function domainFromLocale(domainLocales: Record<string, {
    domain: string | undefined;
}>, url: {
    host: string;
    protocol: string;
}, locale: string, locales?: LocaleObject[]): string | undefined;
/**
 * Returns the locale object with the domain overridden by `domainLocales` runtime config (see also `getHostLocale`),
 * a no-op outside domain setups since the override entries are empty.
 */
export declare function withRuntimeDomain<T extends string | LocaleObject>(locale: T, domainLocales: I18nPublicRuntimeConfig['domainLocales']): T;
